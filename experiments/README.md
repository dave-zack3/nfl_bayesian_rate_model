# Experiment Logs

Each experiment run produces:

- Configuration
- Rolling metrics
- Benchmark metrics
- Timestamp

Saved as:

experiment_YYYYMMDD_HHMMSS.json

This enables reproducibility and performance tracking across model iterations.